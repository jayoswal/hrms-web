# Atlas HRMS Web

React SPA for employees, managers, finance, and HR administrators.

```bash
npm install
npm run dev
```

The Vite server runs on port 3000 and proxies `/api` to the local Atlas
gateway on port 8080.

